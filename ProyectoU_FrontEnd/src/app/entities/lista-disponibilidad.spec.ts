import { ListaDisponibilidad } from './lista-disponibilidad';

describe('ListaDisponibilidad', () => {
  it('should create an instance', () => {
    expect(new ListaDisponibilidad()).toBeTruthy();
  });
});
